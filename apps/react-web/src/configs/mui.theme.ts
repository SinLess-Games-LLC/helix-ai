import { createTheme } from '@mui/material/styles'

export const HelixTheme = createTheme({
  palette: {
    primary: {
      main: 'rgba(1, 17, 91)',
    },
    secondary: {
      main: 'rgba(246, 6, 151)',
    },
  },
})
